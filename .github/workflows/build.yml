name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build with Buildozer
      uses: ArtemSydoryk/buildozer-action@v1.2
      with:
        command: buildozer -v android debug

    - name: Upload Artifact
      uses: actions/upload-artifact@v2
      with:
        name: package
        path: bin/*.apk
